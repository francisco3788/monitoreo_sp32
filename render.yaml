services:
  - type: web
    name: interfaz-web-esp32
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py

  - type: worker
    name: recolector-esp32
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python recolector.py
